-- Keyboard layout. User-owned: edits here survive Ryoku updates.
-- List layouts with `localectl list-x11-keymap-layouts`; variants and options
-- are documented in the Hyprland wiki (input settings).
hl.config({
    input = {
        kb_layout = "us",
        kb_variant = "",
        kb_options = "",
    },
})
