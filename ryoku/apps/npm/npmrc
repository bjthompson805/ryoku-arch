# npm global prefix. The system npm installs `npm i -g` packages under
# /usr/lib/node_modules, which a normal user cannot write (EACCES). Point the
# prefix at ~/.local instead (its bin/ is already on PATH), so global installs
# work without root. ${HOME} is expanded by npm when it reads this file.
prefix=${HOME}/.local
