# Let `pip install --user <pkg>` work on the externally-managed system Python
# (PEP 668). Without this, pip refuses with "externally-managed-environment".
# User installs land in ~/.local (its bin/ is on PATH) and the system
# site-packages stay pacman-managed. For isolated CLI apps, prefer `pipx`.
[global]
break-system-packages = true
