# beta-18 working notes

Not shipped docs. Working state for the UI rework; delete before the branch merges.

- `DESIGN.md`: the design contract. Written to be implemented literally, not read for inspiration.
- `inventory.json`: every setting in the Hub as it exists today (515 across 36 files).
  The rework is checked against this: a setting that is in here and not on screen
  afterwards is a regression, not a simplification.
- `art-grammar.txt`: the fal prompt grammar, plus what it cost to find it.
